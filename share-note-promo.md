# Share Note for Codex

> 把 Markdown 变成可分享的加密页面——预览、发布、读取、更新与删除，都在 Codex 里完成。

**Share Note for Codex 0.1.0** 是一款面向 Codex 的本地效率插件。它将 Share Note 的发布与管理流程带进你的工作区：无需切换工具，无需启动常驻服务，也不依赖 Obsidian。

---

## 从写完，到分享，只差一句话

当文档、方案或技术笔记已经在项目里准备好，你可以直接告诉 Codex：

> “预览这份 Markdown，并加密发布到 Share Note。”

插件会先生成本地预览，检查内容哈希、外部资源与潜在敏感信息；只有在预览通过、发布目标明确，并获得与你当前内容完全匹配的授权后，才会执行上传。

## 一条完整的内容工作流

| 能力 | 你可以做什么 |
|---|---|
| 本地预览 | 在不访问网络的情况下渲染 Markdown 或静态 HTML |
| 加密发布 | 使用新的随机 IV 加密内容，服务端不接收解密密钥 |
| 安全读取 | 通过分享链接或本地记录读取并解密页面 |
| 原位更新 | 重新预览后更新已有页面，并验证回读内容 |
| 本地检索 | 查看插件保存在本机的 Share Note 记录 |
| 可控删除 | 删除远端分享，同时保留本地源文件与审计记录 |

## 安全不是一句口号，而是一组明确边界

- **发布前必预览**：写作和上传是两个独立动作，内容变化后必须重新预览。
- **授权绑定内容哈希**：授权只对当前预览、当前配置与加密模式有效。
- **密钥留在安全存储**：API 凭据与笔记密钥保存在 macOS Keychain，不写入项目文件。
- **拒绝盲目重试**：网络结果不明确时，插件会报告真实状态，避免意外创建重复页面。
- **隔离服务来源**：认证信息只发送到配置中批准的 API 来源，获取分享页时不携带凭据。
- **主动阻断风险内容**：图片、活动嵌入、私钥和疑似凭据会阻止发布，而不是被静默忽略。

## 轻量、透明、适合放进真实项目

Share Note for Codex 使用随插件提供的独立 HTTP 客户端，不会在运行时安装依赖，也不会启动 MCP 服务、守护进程、后台同步或监听器。插件安装目录保持只读；配置、预览、锁与记录写入用户级数据目录。

它不会安装、调用或检查 Obsidian，也不依赖 Vault、URI Handler 或 Obsidian 运行时。

## 三个常用提示词

```text
预览 docs/launch-note.md，先不要上传。

把 docs/launch-note.md 加密发布到已配置的 Share Note。

列出这个项目在本地保存的 Share Note 记录。
```

## 本地安装

在项目仓库根目录运行：

```bash
codex plugin marketplace add "$(pwd)"
codex plugin add share-note@personal
```

安装后新建一个 Codex 对话，让 Skill 被发现。首次使用需要完成 Share Note 凭据导入；插件要求 **Node.js 20+**，当前安全存储实现面向 **macOS Keychain**。

## 当前版本说明

0.1.0 已通过 TypeScript 检查、40 项自动化测试、干净 bundle 运行与插件结构验证。测试覆盖本地安全边界、加密协议、异常写入状态和完整的模拟服务流程。

目标 Share Note 实例的实际兼容性仍取决于它是否符合当前冻结的 `note-sx-client-1.5.5` 协议配置；公开或自托管实例上的线上流程需要使用者在自己的配置环境中验证。

---

## 让好内容更快抵达读者

在 Codex 里完成写作，在 Share Note 上安全分享。流程更短，控制更多，边界更清楚。

**Share Note for Codex — 写完，即可安全分享。**
